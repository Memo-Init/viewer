{
    "note": "Canonical assembled-revision BODY for the EXTERNAL PAYLOAD POINTER render (Memo 080, PRD-D5). Third parity fixture next to revision-body-v1: it covers what the others deliberately never reach \u2014 the GAP render of a `mode: reference` pointer (checksum mismatch / file missing / never measured) and the inline pull-in of a payload-backed block table plus a payload-backed diagram template that is checked BEFORE it is interpolated. SINGLE SOURCE for the cross-repo byte-parity gate: RevisionAssembler (core) renders it and DoltDbAssembler (viewer) must reproduce it byte-for-byte. Regenerate by re-seeding the canonical rows through the production writers (RevisionAssembler.test seedPointerCanonical) and re-hashing full-body.md into both repo copies. sha256 is over full-body.md; a one-sided renderer edit fails its repo parity test (render !== fixture / hash). Memo 080, PRD-R1 Vollausbau: the head now carries SEVEN `| **Feld** | Wert |` rows (the five mandatory ones plus the document level's Typ + Aenderungen), the section sequence is the DECLARED document order (BlockSections.documentSections, REV-18 Z. 158-172) with the seven collective sections in a declared tail, every block carries its toolkit body from the block_section carrier in REGISTER order (absent mandatory/generated headings with the explicit empty mark), and `## Kontaminations-Metadaten` renders from the sessions table. Memo 080, PRD-R2 Vollausbau: the head note is GERMAN and names source, producer and check (`memo-<NNN>.db` / `memo revision assemble` / `memo revision parity`), and the scope line carries the NINE figures of the Umfangszeile in the seven groups of the SHARED template test/fixtures/revision-scope-line-v1.json \u2014 a figure whose carrier is absent renders `nicht ausweisbar`, never 0. The seed therefore also fills block_chapter, prd and topic.status, which are NOT rendered anywhere else: without them both renderers would only agree on a row of zeros.",
    "fixture": "full-body.md",
    "sha256": "5a8265087eac2d7ab0f82b28eff4fc3c38dcb742621ad9227ebc3082893bba52",
    "byteLength": 1857
}
