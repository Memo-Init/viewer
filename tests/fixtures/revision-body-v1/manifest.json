{
    "note": "Canonical assembled-revision BODY (Memo 079, PRD-16 answerable questions + Slice-2b Research register; Memo 080, PRD-R1 head table + six mandatory prose sections + snag/goal/maintenance sections; Memo 080, PRD-R2 the VISIBLE generation note + the scope line in the head). SINGLE SOURCE for the cross-repo byte-parity gate: RevisionAssembler (core) freezes it and DoltDbAssembler (viewer) must reproduce it byte-for-byte. The questions-json fence carries the FULL answerable canonical shape (title/hintergrund/frage/aiRecommendation/typ/options[]); the ## Research section renders the memo-local research + research_topics + research_files edges; the head renders the generation note, the scope line (one figure per content carrier — never revision/provenance/history_journal, which are written after the render), the five mandatory `| **Feld** | Wert |` rows, and the six prose sections come from the memo_section carrier. Regenerate by re-seeding the canonical rows through the production writers (RevisionAssembler.test seedCanonical / DoltDbAssembler.test seedFullCanonical) and re-hashing full-body.md into both repo copies. sha256 is over full-body.md; a one-sided renderer edit fails its repo parity test (render !== fixture / hash).",
    "fixture": "full-body.md",
    "sha256": "29ad5a4ea4cc9921ee1f142a93327138c07eabc0616509253b7fe3c0fa8c9325",
    "byteLength": 3363
}
