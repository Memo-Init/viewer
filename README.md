# memo-init / viewer

The **memo-viewer** — a local live-preview server (port 3333) for memos, revisions and transcripts,
with Mermaid-SVG rendering and the deterministic `questions-json` question format.

Extracted from `cli/memo-toolkit/editor` (Memo 001, Kap 27). Core-like: a direct dependency of the spec.

> Skeleton — content is populated by rollout Phase 7 (extraction + security check + README).

License: MIT
